# Go Web app template (with React + TypeScript)

Minimal boilerplate for a Web app built using Go, TypeScript, and React.

To run a development server and automatically restart it when files
change:

```
./dev.sh
```
